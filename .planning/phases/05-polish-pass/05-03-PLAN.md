---
phase: 05-polish-pass
plan: 03
type: execute
wave: 2
depends_on:
  - 05-02
files_modified:
  - app/views/leagues/index.html.slim
  - app/views/dashboard/index.html.slim
autonomous: false
requirements:
  - POLL-02

must_haves:
  truths:
    - "Cards in app/views/leagues/index.html.slim that contain a stretched-link have the .card-hoverable class"
    - "Cards in app/views/dashboard/index.html.slim 'Последние лиги' grid (which contain stretched-link) have the .card-hoverable class"
    - "Hovering a card on /leagues lifts it 1px and brightens its border within 150ms — visible in the browser"
    - "Mousing out reverses the lift smoothly (animates back, does not snap) — proves the transition on .card base rule is working"
    - "Stat cards on dashboard (no stretched-link) do NOT receive .card-hoverable — they are not interactive"
    - "League/tournament wrapper cards on dashboard ('Мои лиги', 'Мои турниры' card-headers, no stretched-link) do NOT receive .card-hoverable"
  artifacts:
    - path: "app/views/leagues/index.html.slim"
      provides: "Card grid with hoverable class on each league card"
      contains: ".card.card-hoverable.h-100"
    - path: "app/views/dashboard/index.html.slim"
      provides: "Recent leagues grid with hoverable class"
      contains: ".card.card-hoverable.h-100"
  key_links:
    - from: "app/views/leagues/index.html.slim card"
      to: "_cards.scss .card-hoverable:hover rule"
      via: "class attribute on Slim div"
      pattern: "\\.card\\.card-hoverable"
    - from: "app/views/dashboard/index.html.slim recent leagues card"
      to: "_cards.scss .card-hoverable:hover rule"
      via: "class attribute on Slim div"
      pattern: "\\.card\\.card-hoverable"
---

<objective>
Implement POLL-02 (template half) — apply the `.card-hoverable` class (defined in Plan 05-02) to the cards that should react to hover.

Purpose: After Plan 05-02, `.card-hoverable:hover` exists in CSS but matches zero elements in the DOM. This plan makes the hover lift visible TODAY by adding the class to stretched-link cards on the leagues index and dashboard. Per user decision (CONTEXT lock), the targets are cards with `stretched-link` children that should be interactive.

Scope verified by codebase grep: `grep -rln "stretched-link" app/views/` returns exactly two files — `app/views/leagues/index.html.slim` and `app/views/dashboard/index.html.slim`. No other templates need editing.

Output: Two Slim templates updated; manual browser verification of the hover lift effect.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-polish-pass/05-RESEARCH.md
@.planning/phases/05-polish-pass/05-UI-SPEC.md
@.planning/phases/05-polish-pass/05-02-SUMMARY.md
@app/views/leagues/index.html.slim
@app/views/dashboard/index.html.slim
@app/assets/stylesheets/_cards.scss

<interfaces>
<!-- The CSS rule from Plan 05-02 (already committed before this plan runs): -->

```scss
// _cards.scss
.card-hoverable:hover {
  transform: translateY(-1px);
  border-color: var(--color-accent-hover);
}
```

<!-- Existing Slim card pattern in templates (Slim shorthand: tag.class.class -> <div class="class class">): -->

```slim
.card.h-100        # current — renders <div class="card h-100">
.card-body
  h5.card-title= link_to ..., class: "stretched-link"
```

<!-- After this plan: -->

```slim
.card.card-hoverable.h-100   # renders <div class="card card-hoverable h-100">
.card-body
  h5.card-title= link_to ..., class: "stretched-link"
```

CONTEXT LOCK (user decision): apply `.card-hoverable` ONLY to cards that have a `stretched-link` child AND should be interactive. Do NOT add it to:
- Stat cards on dashboard (lines 7, 13 of dashboard/index.html.slim) — no stretched-link, not interactive.
- Wrapper cards "Мои лиги" / "Мои турниры" (lines 22, 38 of dashboard/index.html.slim) — no stretched-link, contain list-group items with their own links.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add .card-hoverable to leagues/index.html.slim card grid</name>
  <files>app/views/leagues/index.html.slim</files>
  <read_first>
    - app/views/leagues/index.html.slim (current 21 lines — confirm line 10 is `.card.h-100` inside the @leagues.each loop)
    - app/assets/stylesheets/_cards.scss (confirm `.card-hoverable:hover` rule exists from Plan 05-02; if missing, STOP and report — Plan 05-02 must run first)
  </read_first>
  <action>
Edit `app/views/leagues/index.html.slim`. Change EXACTLY ONE line:

Line 10 currently reads:
```
          .card.h-100
```

Change it to:
```
          .card.card-hoverable.h-100
```

Slim shorthand semantics: `.card.card-hoverable.h-100` produces `<div class="card card-hoverable h-100">`. Class order in the rendered HTML is the same as written; CSS rule `.card-hoverable:hover` matches regardless of class order.

Critical constraints:
- Do NOT change indentation. The line keeps its existing 10-space indent (it's inside `.col` which is inside `.row.row-cols-...` which is inside `- @leagues.each do |league|`).
- Do NOT remove the `.h-100` class — it equalizes card heights in the grid.
- Insert `.card-hoverable` BETWEEN `.card` and `.h-100` (the order does not matter for CSS, but consistent ordering helps readability and matches the dashboard edit in Task 2).
- Do NOT modify any other line — leave the .card-body, h5.card-title, stretched-link, .card-footer blocks exactly as they are. The stretched-link is what makes the entire card click-target; the class addition only adds the hover animation.
- Do NOT touch the `- else` empty state branch.
  </action>
  <verify>
    <automated>grep -c "\.card\.card-hoverable\.h-100" app/views/leagues/index.html.slim | grep -qx 1 &amp;&amp; ! grep -E "^\s*\.card\.h-100\s*$" app/views/leagues/index.html.slim &amp;&amp; grep -q "stretched-link" app/views/leagues/index.html.slim &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `app/views/leagues/index.html.slim` contains the literal string `.card.card-hoverable.h-100` exactly once.
    - File does NOT contain a bare `.card.h-100` line (without `.card-hoverable`) — the original card class string has been replaced.
    - File still contains `stretched-link` (proves we did not accidentally remove the link).
    - File still contains `card-body`, `card-title`, `card-footer` (existing structure preserved).
    - Total line count of the file is unchanged (21 lines) — we modified one line, not added/removed any.
  </acceptance_criteria>
  <done>The leagues index card-grid card has the `.card-hoverable` class; grep verification passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add .card-hoverable to dashboard/index.html.slim recent leagues grid</name>
  <files>app/views/dashboard/index.html.slim</files>
  <read_first>
    - app/views/dashboard/index.html.slim (current 66 lines — confirm line 58 is `.card.h-100` inside the @recent_leagues.each loop, AFTER the "Последние лиги" h5)
    - .planning/phases/05-polish-pass/05-RESEARCH.md (Open Questions section — confirms only stretched-link cards qualify)
  </read_first>
  <action>
Edit `app/views/dashboard/index.html.slim`. Change EXACTLY ONE line.

Line 58 currently reads:
```
            .card.h-100
```

(This line is inside the `@recent_leagues.each do |league|` block, under the `h5.mb-3 Последние лиги` heading. The card it defines wraps a `.card-body` containing an h6 with a `stretched-link`.)

Change it to:
```
            .card.card-hoverable.h-100
```

Critical constraints — DO NOT modify any other card in this file:
- Lines 7, 13 (stat cards: `.card` containing icons + counts, NO stretched-link) — leave UNCHANGED. Stat cards display data only and are not navigable.
- Line 22 (`.card.h-100` for "Мои лиги" wrapper containing card-header + list-group + card-footer with buttons) — leave UNCHANGED. This card has no stretched-link; its inner list items have their own per-item links and the card itself is not click-targetable.
- Line 38 (`.card.h-100` for "Мои турниры" wrapper) — leave UNCHANGED. Same reasoning as line 22.
- Line 58 (`.card.h-100` inside @recent_leagues.each, contains stretched-link on h6.card-title at line 60) — THIS is the only line to change.

Indentation: line 58 has 12 spaces of indent (it's inside `.col` inside `.row.row-cols-...` inside `- @recent_leagues.each do |league|`). Preserve this indent exactly.

Slim shorthand: `.card.card-hoverable.h-100` renders as `<div class="card card-hoverable h-100">`.

Verification of "no other cards changed": after the edit, the file should still contain exactly 4 occurrences of `.card.h-100` becoming 3 (lines 22, 38, and... wait — let me recount). Re-check: original file has `.card.h-100` on lines 22, 38, 58 (three occurrences) and bare `.card` (without h-100) on lines 7, 13 (two occurrences — stat cards). After the edit, `.card.h-100` appears on 2 lines (22, 38) and `.card.card-hoverable.h-100` appears on 1 line (58). Total `.card`-prefixed lines: 5 (unchanged from before).
  </action>
  <verify>
    <automated>grep -c "\.card\.card-hoverable\.h-100" app/views/dashboard/index.html.slim | grep -qx 1 &amp;&amp; grep -c "^[[:space:]]*\.card\.h-100[[:space:]]*$" app/views/dashboard/index.html.slim | grep -qx 2 &amp;&amp; grep -c "^[[:space:]]*\.card[[:space:]]*$" app/views/dashboard/index.html.slim | grep -qx 2 &amp;&amp; grep -q "Последние лиги" app/views/dashboard/index.html.slim &amp;&amp; grep -q "stretched-link" app/views/dashboard/index.html.slim &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `app/views/dashboard/index.html.slim` contains the literal string `.card.card-hoverable.h-100` exactly once.
    - File contains exactly two remaining bare `.card.h-100` lines (the "Мои лиги" and "Мои турниры" wrappers — lines 22 and 38).
    - File contains exactly two bare `.card` lines (without `.h-100`, without `.card-hoverable`) — the two stat cards (lines 7, 13).
    - File still contains the heading `Последние лиги`.
    - File still contains `stretched-link` (proves the recent-leagues card link is preserved).
    - Total line count unchanged (66 lines).
    - The `.card-hoverable` class is ONLY on the recent leagues card — verifiable by checking that the lines immediately after the new class line still reference `@recent_leagues` context (`.card-body`, then h6 with stretched-link to `league_path(league)`).
  </acceptance_criteria>
  <done>Only the "Последние лиги" recent-leagues card has `.card-hoverable`; stat cards and "Мои лиги/турниры" wrappers are untouched; grep verification passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verifies hover lift is visible in browser</name>
  <files></files>
  <read_first>
    - app/views/leagues/index.html.slim (confirm Task 1 changes)
    - app/views/dashboard/index.html.slim (confirm Task 2 changes)
  </read_first>
  <what-built>
The `.card-hoverable` class has been added to all stretched-link cards (leagues index grid + dashboard "Последние лиги" grid). Combined with the `.card-hoverable:hover` rule from Plan 05-02 and the base `.card` transition, hovering these cards should now produce a 1px upward lift and a border-color brighten to `#4d94f8`, animating in over 150ms and animating back out on mouseout.
  </what-built>
  <how-to-verify>
1. Start the dev server (if not running): `bin/rails s`.
2. Open the app in a browser (typically http://localhost:3000).
3. Navigate to **/leagues**:
   - There should be at least one league card visible in the grid (or seed data if needed). If the grid is empty ("Лиг пока нет."), create one league via "Новая лига" so a card appears.
   - Hover one of the cards with the mouse. Expected: the card lifts up by 1px and its border brightens from dark gray (#30363d) to a brighter blue (#4d94f8). Both changes animate over ~150ms.
   - Move the mouse off the card. Expected: the lift and border color reverse smoothly back to the default state — should NOT snap instantly.
4. Navigate to **/** (dashboard, root path):
   - Scroll to the "Последние лиги" section near the bottom (only visible if there are recent leagues; create one if empty).
   - Hover one of the cards in that grid. Expected: same lift + border-brighten effect.
   - Confirm the STAT CARDS at the top ("Мои лиги" / "Мои турниры" with icon + count) do NOT lift on hover — they are intentionally non-interactive.
   - Confirm the LARGE wrapper cards ("Мои лиги" with list-group, "Мои турниры" with list-group) do NOT lift on hover either — only their inner list-item links should be hoverable.
5. POLL-03 cross-check while you're here: visit any league with members or tournaments (e.g. /leagues/1) — table column headers should now appear in UPPERCASE with extra letter-spacing. (This is from Plan 05-02 / POLL-03; confirming it still works end-to-end.)
6. POLL-01 cross-check: hover the "Новая лига" button (or any `.btn-primary`) — expected: soft electric blue glow shadow on hover. (From Plan 05-01.)

If any of the above does not behave as described, type a description of what went wrong. Otherwise, type "approved" to mark the phase visually complete.
  </how-to-verify>
  <acceptance_criteria>
    - User confirms: hovering a card on /leagues produces a visible upward lift (~1px) AND a brighter border, both animating smoothly over ~150ms.
    - User confirms: mousing off the card reverses the effect smoothly (does not snap).
    - User confirms: dashboard stat cards and wrapper cards do NOT lift (only "Последние лиги" cards do).
    - User confirms: table headers (POLL-03) render uppercase with letter-spacing.
    - User confirms: primary buttons (POLL-01) glow on hover and on keyboard focus.
    - User types "approved" or describes any issue.
  </acceptance_criteria>
  <resume-signal>Type "approved" to confirm all three POLL requirements are visually working, or describe the issue (e.g. "card snaps back instead of animating", "lift not visible at all").</resume-signal>
  <done>User has verified all three POLL requirements work end-to-end in the browser.</done>
</task>

</tasks>

<verification>
After Tasks 1 and 2, run these checks:

```bash
# Task 1
grep -c "\.card\.card-hoverable\.h-100" app/views/leagues/index.html.slim   # expect: 1
! grep -qE "^\s*\.card\.h-100\s*$" app/views/leagues/index.html.slim         # expect: bare .card.h-100 gone

# Task 2 — selective application
grep -c "\.card\.card-hoverable\.h-100" app/views/dashboard/index.html.slim  # expect: 1
grep -cE "^\s*\.card\.h-100\s*$" app/views/dashboard/index.html.slim         # expect: 2 (Мои лиги, Мои турниры)
grep -cE "^\s*\.card\s*$" app/views/dashboard/index.html.slim                 # expect: 2 (stat cards)

# Phase 5 cumulative
grep -rln "stretched-link" app/views/                                          # expect: leagues/index, dashboard/index
grep -rln "card-hoverable" app/views/                                          # expect: same two files
```

Task 3 is human-verified; no automated check.
</verification>

<success_criteria>
- POLL-02 fully satisfied — ROADMAP.md success criterion: "Hovering a clickable card animates a `translateY(-1px)` lift and brightens the border within 150ms, then reverses on mouse-out" — visible in browser on /leagues and dashboard "Последние лиги" grids.
- Stat cards and wrapper cards intentionally NOT hoverable (per user decision: only stretched-link cards get `.card-hoverable`).
- All three Phase 5 POLL requirements (POLL-01, POLL-02, POLL-03) are now end-to-end visible to the user.
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-pass/05-03-SUMMARY.md` documenting:
- Files modified: `app/views/leagues/index.html.slim` (1 line), `app/views/dashboard/index.html.slim` (1 line)
- POLL-02 visually verified end-to-end
- Cross-check that POLL-01 and POLL-03 still work
- Note on selective application: stat cards and wrapper cards not given `.card-hoverable` per user decision
</output>
</content>
</invoke>