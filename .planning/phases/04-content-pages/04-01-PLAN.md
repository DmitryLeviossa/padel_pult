---
phase: 04-content-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/dashboard/index.html.slim
autonomous: true
requirements:
  - PAGE-01

must_haves:
  truths:
    - "Dashboard shows two stat cards: 'Мои лиги' with count and people icon, 'Мои турниры' with count and trophy icon"
    - "The stat-card row appears between the h1 heading and the .row.g-4 section"
    - "No .shadow-sm class appears anywhere in dashboard/index.html.slim"
    - "Empty states in My Leagues and My Tournaments cards use p.text-muted.mb-0"
    - "Recent leagues card grid uses bare .card (no .shadow-sm)"
  artifacts:
    - path: "app/views/dashboard/index.html.slim"
      provides: "Redesigned dashboard with stat cards, card grid, recent leagues"
      contains: "bi-people"
  key_links:
    - from: "stat card row"
      to: "@my_leagues.count / @my_tournaments.count"
      via: "direct interpolation in .fs-4.fw-semibold= @my_leagues.count"
      pattern: "@my_leagues\\.count"
    - from: ".card.shadow-sm"
      to: "removed"
      via: "shadow suppressed in _theme.scss"
      pattern: "shadow-sm"
---

<objective>
Redesign the dashboard template: add a stat-card row with Bootstrap Icons and remove all .shadow-sm classes.

Purpose: The dashboard is the first screen users see after login. It must display at-a-glance stats and a card grid that feel native to the dark design system.
Output: Updated app/views/dashboard/index.html.slim with stat cards, clean card surfaces, correct empty states.
</objective>

<execution_context>
@/Users/dmitry_gusev/Work/GK_Pult/padel_pult/.planning/phases/04-content-pages/04-RESEARCH.md
@/Users/dmitry_gusev/Work/GK_Pult/padel_pult/.planning/phases/04-content-pages/04-PATTERNS.md
</execution_context>

<context>
@/Users/dmitry_gusev/Work/GK_Pult/padel_pult/.planning/ROADMAP.md

<interfaces>
<!-- Controller variables available in dashboard/index — DO NOT reference any others -->
<!-- Source: app/controllers/dashboard_controller.rb -->
@my_leagues    = League.where(owner: current_user)
@my_tournaments = Tournament.joins(:league).where(...)  # user's tournaments via owned leagues
@recent_leagues = League.order(created_at: :desc).limit(5)

<!-- Bootstrap Icons already imported in application.bootstrap.scss -->
<!-- Use: i.bi.bi-iconname.fs-2.text-primary -->
<!-- Available: bi-people (leagues), bi-trophy (tournaments) -->

<!-- _cards.scss sets: background-color: var(--color-bg-surface); no shadow -->
<!-- .shadow-sm is suppressed to none in _theme.scss — remove it from markup for clarity -->

<!-- Current file state (lines 1-49): -->
.container.py-4
  h1.mb-4 Добро пожаловать, #{current_user.full_name}!

  .row.g-4
    .col-md-6
      .card.shadow-sm.h-100         ← line 6: drop .shadow-sm
        .card-header.fw-semibold Мои лиги
        ...
    .col-md-6
      .card.shadow-sm.h-100         ← line 22: drop .shadow-sm
        .card-header.fw-semibold Мои турниры
        ...

  .mt-5
    h5.mb-3 Последние лиги
    - if @recent_leagues.any?
      .row.row-cols-1.row-cols-md-3.g-3
        - @recent_leagues.each do |league|
          .col
            .card.shadow-sm.h-100   ← line 42: drop .shadow-sm
              .card-body
                h6.card-title= link_to ...
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add stat-card row and remove all .shadow-sm from dashboard</name>
  <files>app/views/dashboard/index.html.slim</files>
  <read_first>
    - app/views/dashboard/index.html.slim (current state — read before modifying)
    - app/assets/stylesheets/_theme.scss (confirms shadow suppression is already in place)
  </read_first>
  <action>
Rewrite app/views/dashboard/index.html.slim with these exact changes:

1. INSERT a stat-card row BETWEEN line 2 (h1.mb-4) and line 4 (.row.g-4). The new block:

```slim
  .row.g-3.mb-4
    .col-sm-6.col-lg-3
      .card
        .card-body.d-flex.align-items-center.gap-3
          i.bi.bi-people.fs-2.text-primary
          div
            .text-muted.small Мои лиги
            .fs-4.fw-semibold= @my_leagues.count
    .col-sm-6.col-lg-3
      .card
        .card-body.d-flex.align-items-center.gap-3
          i.bi.bi-trophy.fs-2.text-primary
          div
            .text-muted.small Мои турниры
            .fs-4.fw-semibold= @my_tournaments.count
```

2. Line 6: change `.card.shadow-sm.h-100` to `.card.h-100`
3. Line 22: change `.card.shadow-sm.h-100` to `.card.h-100`
4. Line 42: change `.card.shadow-sm.h-100` to `.card.h-100`

All other content (list-group items, card-footer buttons, recent leagues section, empty states) must remain EXACTLY as-is — do not alter them.

IMPORTANT — only these two controller variables are valid for stat cards:
- @my_leagues.count
- @my_tournaments.count
Do NOT add @recent_leagues.count or any other variable to the stat cards.
  </action>
  <verify>
    <automated>grep -c "shadow-sm" /Users/dmitry_gusev/Work/GK_Pult/padel_pult/app/views/dashboard/index.html.slim</automated>
    Expected output: 0
    Also run: grep -n "bi-people\|bi-trophy\|@my_leagues.count\|@my_tournaments.count" /Users/dmitry_gusev/Work/GK_Pult/padel_pult/app/views/dashboard/index.html.slim
    Expected: lines containing each pattern exist
  </verify>
  <acceptance_criteria>
    - `grep -c "shadow-sm" app/views/dashboard/index.html.slim` returns 0
    - `grep -c "bi-people" app/views/dashboard/index.html.slim` returns 1
    - `grep -c "bi-trophy" app/views/dashboard/index.html.slim` returns 1
    - `grep -c "my_leagues.count" app/views/dashboard/index.html.slim` returns 1
    - `grep -c "my_tournaments.count" app/views/dashboard/index.html.slim` returns 1
    - `grep -c "row.g-3.mb-4" app/views/dashboard/index.html.slim` returns 1 (stat row wrapper)
    - `grep -c "col-sm-6.col-lg-3" app/views/dashboard/index.html.slim` returns 2 (one per stat card)
    - File still contains `.card.h-100` (not `.card.shadow-sm.h-100`) in lines for My Leagues and My Tournaments sections
    - File still contains `.card-header.fw-semibold Мои лиги` unchanged
    - File still contains `p.text-muted.mb-0 Вы ещё не создали ни одной лиги.` unchanged (empty state)
    - File still contains `link_to "Все лиги", leagues_path` in card footer unchanged
  </acceptance_criteria>
  <done>Dashboard template has a 2-stat card row with icons and counts; all 3 card.shadow-sm occurrences removed; all existing content preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Controller → Template | @my_leagues, @my_tournaments, @recent_leagues are ActiveRecord scoped to current_user — already auth-gated at controller level |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Information Disclosure | dashboard/index.html.slim | accept | Template only renders data from controller-scoped variables. current_user scoping enforced at controller — no template change affects this. |
</threat_model>

<verification>
After task completes, run:
```
grep -c "shadow-sm" app/views/dashboard/index.html.slim
# Expected: 0

grep -n "bi-people\|bi-trophy" app/views/dashboard/index.html.slim
# Expected: 2 lines found

grep -n "my_leagues.count\|my_tournaments.count" app/views/dashboard/index.html.slim
# Expected: 2 lines found

grep -n "row.g-3.mb-4" app/views/dashboard/index.html.slim
# Expected: 1 line found
```
</verification>

<success_criteria>
- dashboard/index.html.slim has 0 occurrences of shadow-sm
- Stat-card row exists with bi-people + @my_leagues.count and bi-trophy + @my_tournaments.count
- Stat-card row is nested inside .container.py-4, positioned after h1.mb-4 and before .row.g-4
- All existing card content (list-group, footer buttons, recent leagues section) is preserved unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/04-content-pages/04-01-SUMMARY.md`
</output>
