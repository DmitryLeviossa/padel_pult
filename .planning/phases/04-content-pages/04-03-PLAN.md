---
phase: 04-content-pages
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/leagues/index.html.slim
  - app/views/leagues/new.html.slim
  - app/views/leagues/edit.html.slim
autonomous: true
requirements:
  - PAGE-03

must_haves:
  truths:
    - "leagues/index has no .shadow-sm class — card grid uses bare .card.h-100"
    - "leagues/new is wrapped in .container.py-4 — form no longer bleeds to browser edge"
    - "leagues/edit is wrapped in .container.py-4 — form no longer bleeds to browser edge"
    - "leagues/show requires no changes and is left untouched"
  artifacts:
    - path: "app/views/leagues/index.html.slim"
      provides: "League card grid — no shadow"
      contains: ".card.h-100"
    - path: "app/views/leagues/new.html.slim"
      provides: "New league form — container-wrapped"
      contains: ".container.py-4"
    - path: "app/views/leagues/edit.html.slim"
      provides: "Edit league form — container-wrapped"
      contains: ".container.py-4"
  key_links:
    - from: "leagues/index .card.h-100"
      to: "_cards.scss dark surface"
      via: "Bootstrap CSS cascade from data-bs-theme=dark"
      pattern: "\\.card\\.h-100(?!\\.shadow)"
    - from: "leagues/new .container.py-4"
      to: "main.py-4 in layout"
      via: "internal horizontal constraint convention (Phase 2 decision)"
      pattern: "\\.container\\.py-4"
---

<objective>
Redesign 3 of 4 league templates: remove .shadow-sm from index card grid, add .container.py-4 wrapper to new and edit forms. leagues/show is already complete and must not be touched.

Purpose: League pages are the core feature surface. Index must use clean dark cards; form pages must not bleed to the browser edge.
Output: 3 updated league templates conforming to Phase 3 card and container conventions.
</objective>

<execution_context>
@/Users/dmitry_gusev/Work/GK_Pult/padel_pult/.planning/phases/04-content-pages/04-RESEARCH.md
@/Users/dmitry_gusev/Work/GK_Pult/padel_pult/.planning/phases/04-content-pages/04-PATTERNS.md
</execution_context>

<context>
@/Users/dmitry_gusev/Work/GK_Pult/padel_pult/.planning/ROADMAP.md

<interfaces>
<!-- leagues/index current state (lines 1-21, full file): -->
.container.py-4
  .d-flex.justify-content-between.align-items-center.mb-4
    h1.mb-0 Лиги
    = link_to "Новая лига", new_league_path, class: "btn btn-primary"

  - if @leagues.any?
    .row.row-cols-1.row-cols-md-2.row-cols-lg-3.g-4
      - @leagues.each do |league|
        .col
          .card.h-100.shadow-sm    ← line 10: change to .card.h-100
            ...
  - else
    p.text-muted Лиг пока нет.

<!-- leagues/new current state (lines 1-20, full file): -->
h1 Новая лига                          ← NO container — line 1 is bare h1

= form_with model: @league, class: "mt-4" do |f|
  ...

<!-- leagues/edit current state (lines 1-23, full file): -->
h1 Редактировать лигу                  ← NO container — line 1 is bare h1

= form_with model: @league, class: "mt-4" do |f|
  ...

<!-- Container pattern (from tournaments/new.html.slim, the reference analog): -->
.container.py-4
  h1.mb-4 Page Title

  = form_with model: @object, class: "mt-4" do |f|
    / ... form fields

<!-- leagues/show — DO NOT TOUCH. Already complete from Phase 3. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove .shadow-sm from leagues/index card grid</name>
  <files>app/views/leagues/index.html.slim</files>
  <read_first>
    - app/views/leagues/index.html.slim (current state — read before modifying)
  </read_first>
  <action>
In app/views/leagues/index.html.slim, make exactly 1 change:

Line 10: `.card.h-100.shadow-sm` → `.card.h-100`

All other content (container wrapper, d-flex header, row-cols grid, card-body, card-footer, empty state) must remain EXACTLY as-is.
  </action>
  <verify>
    <automated>grep -c "shadow-sm" /Users/dmitry_gusev/Work/GK_Pult/padel_pult/app/views/leagues/index.html.slim</automated>
    Expected output: 0
  </verify>
  <acceptance_criteria>
    - `grep -c "shadow-sm" app/views/leagues/index.html.slim` returns 0
    - `grep -c "card.h-100" app/views/leagues/index.html.slim` returns 1 (card preserved without shadow)
    - `grep -c "container.py-4" app/views/leagues/index.html.slim` returns 1 (outer wrapper unchanged)
    - `grep -c "row-cols-1.row-cols-md-2.row-cols-lg-3.g-4" app/views/leagues/index.html.slim` returns 1 (grid unchanged)
    - `grep -c "Лиг пока нет" app/views/leagues/index.html.slim` returns 1 (empty state unchanged)
    - `grep -c "Новая лига" app/views/leagues/index.html.slim` returns 1 (header button unchanged)
  </acceptance_criteria>
  <done>leagues/index: line 10 changed from .card.h-100.shadow-sm to .card.h-100; all other content unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add .container.py-4 wrapper to leagues/new and leagues/edit</name>
  <files>
    app/views/leagues/new.html.slim,
    app/views/leagues/edit.html.slim
  </files>
  <read_first>
    - app/views/leagues/new.html.slim (current state — confirm it starts with bare h1 on line 1)
    - app/views/leagues/edit.html.slim (current state — confirm it starts with bare h1 on line 1)
    - app/views/tournaments/new.html.slim (reference pattern for container-wrapped form pages)
  </read_first>
  <action>
For BOTH leagues/new.html.slim AND leagues/edit.html.slim, wrap the entire file content inside a .container.py-4 block. Apply the same indent change to every existing line.

leagues/new.html.slim — NEW full file content:
```slim
.container.py-4
  h1.mb-4 Новая лига

  = form_with model: @league, class: "mt-4" do |f|
    .mb-3
      = f.label :name, class: "form-label"
      = f.text_field :name, class: "form-control", required: true
      - if @league.errors[:name].any?
        .text-danger.small= @league.errors[:name].first

    .mb-3
      = f.label :description, class: "form-label"
      = f.text_area :description, class: "form-control", rows: 4

    .mb-3
      = f.label :logo, "Логотип", class: "form-label"
      = f.file_field :logo, class: "form-control", accept: "image/*"

    = f.submit "Создать лигу", class: "btn btn-primary"
    = link_to "Отмена", leagues_path, class: "btn btn-secondary ms-2"
```

Note: The original h1 becomes `h1.mb-4` (add `.mb-4` for bottom spacing matching project convention). The form_with `class: "mt-4"` stays as-is.

leagues/edit.html.slim — NEW full file content:
```slim
.container.py-4
  h1.mb-4 Редактировать лигу

  = form_with model: @league, class: "mt-4" do |f|
    .mb-3
      = f.label :name, class: "form-label"
      = f.text_field :name, class: "form-control", required: true
      - if @league.errors[:name].any?
        .text-danger.small= @league.errors[:name].first

    .mb-3
      = f.label :description, class: "form-label"
      = f.text_area :description, class: "form-control", rows: 4

    .mb-3
      = f.label :logo, "Логотип", class: "form-label"
      - if @league.logo.attached?
        .mb-2
          = image_tag @league.logo, class: "rounded", style: "width: 80px; height: 80px; object-fit: cover;"
      = f.file_field :logo, class: "form-control", accept: "image/*"

    = f.submit "Сохранить", class: "btn btn-primary"
    = link_to "Отмена", @league, class: "btn btn-secondary ms-2"
```

Note: The original h1 becomes `h1.mb-4`. The logo image_tag, file_field, form_with model, and all other form fields must be reproduced exactly — no text, helper arguments, or class names may be altered.
  </action>
  <verify>
    <automated>
      grep -c "container.py-4" /Users/dmitry_gusev/Work/GK_Pult/padel_pult/app/views/leagues/new.html.slim && grep -c "container.py-4" /Users/dmitry_gusev/Work/GK_Pult/padel_pult/app/views/leagues/edit.html.slim
    </automated>
    Expected: 1 and 1
  </verify>
  <acceptance_criteria>
    - `grep -c "container.py-4" app/views/leagues/new.html.slim` returns 1
    - `grep -c "container.py-4" app/views/leagues/edit.html.slim` returns 1
    - `grep -c "^h1" app/views/leagues/new.html.slim` returns 0 (h1 is now indented under .container, not at column 0)
    - `grep -c "^h1" app/views/leagues/edit.html.slim` returns 0 (same — h1 now indented)
    - `grep -c "h1.mb-4 Новая лига" app/views/leagues/new.html.slim` returns 1
    - `grep -c "h1.mb-4 Редактировать лигу" app/views/leagues/edit.html.slim` returns 1
    - `grep -c "form_with model: @league" app/views/leagues/new.html.slim` returns 1 (form helper unchanged)
    - `grep -c "form_with model: @league" app/views/leagues/edit.html.slim` returns 1
    - `grep -c "Создать лигу" app/views/leagues/new.html.slim` returns 1 (submit unchanged)
    - `grep -c "Сохранить" app/views/leagues/edit.html.slim` returns 1 (submit unchanged)
    - `grep -c "@league.logo.attached" app/views/leagues/edit.html.slim` returns 1 (logo preview preserved)
  </acceptance_criteria>
  <done>leagues/new and leagues/edit: both start with .container.py-4, h1 updated to h1.mb-4, all form content indented and preserved exactly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authenticated user → League forms | form_with uses Rails UJS CSRF tokens automatically — no template change affects CSRF protection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | leagues/new.html.slim, leagues/edit.html.slim | accept | Changes add a wrapper div and adjust h1 class only. form_with model: @league and all field helpers are unchanged — Rails still generates CSRF tokens and correct action URLs. |
</threat_model>

<verification>
After tasks complete:
```
grep -rn "shadow-sm" app/views/leagues/
# Expected: 0 results (leagues/show already had no shadow-sm)

grep -n "container.py-4" app/views/leagues/new.html.slim app/views/leagues/edit.html.slim
# Expected: 2 results (line 1 of each file)

grep -n "^h1\|^h2" app/views/leagues/new.html.slim app/views/leagues/edit.html.slim
# Expected: 0 results (h1 now indented under .container.py-4)
```
</verification>

<success_criteria>
- leagues/index: 0 occurrences of shadow-sm; .card.h-100 preserved
- leagues/new: .container.py-4 wrapper on line 1; h1.mb-4 inside container; all form fields unchanged
- leagues/edit: .container.py-4 wrapper on line 1; h1.mb-4 inside container; logo preview + all form fields unchanged
- leagues/show: file not modified (already complete — verify by checking git diff shows no change to leagues/show.html.slim)
</success_criteria>

<output>
After completion, create `.planning/phases/04-content-pages/04-03-SUMMARY.md`
</output>
