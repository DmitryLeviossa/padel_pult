---
phase: 04-content-pages
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/users/index.html.slim
autonomous: true
requirements:
  - PAGE-05

must_haves:
  truths:
    - "users/index table has no .table-striped class — uses .table.table-hover.align-middle"
    - "users/index table has an Email column header (th Email) and email data cell (td= user.email)"
    - "users/index still renders the avatar partial in the Фото column"
    - "Empty state p.text-muted is unchanged"
  artifacts:
    - path: "app/views/users/index.html.slim"
      provides: "Users index — dark table with avatar, name, email, registration date"
      contains: "user.email"
  key_links:
    - from: "table.table.table-hover.align-middle"
      to: "_tables.scss dark thead"
      via: "Bootstrap CSS cascade — thead styled by Phase 3 _tables.scss var(--color-bg-surface)"
      pattern: "table\\.table\\.table-hover\\.align-middle"
    - from: "render shared/avatar"
      to: "_avatar.html.slim"
      via: "Rails partial render — user: user passed correctly"
      pattern: "render.*shared/avatar.*user: user"
---

<objective>
Apply two changes to users/index.html.slim: remove .table-striped and add an Email column. PAGE-05 requires "dark table with avatar column, name, email" — the email column is currently missing.

Purpose: The users index must match the component library table pattern (no striped rows) and display the email field specified in the requirement.
Output: Updated users/index.html.slim with .table.table-hover.align-middle (no .table-striped), plus Email column in thead and tbody.
</objective>

<execution_context>
@/Users/dmitry_gusev/Work/GK_Pult/padel_pult/.planning/phases/04-content-pages/04-RESEARCH.md
@/Users/dmitry_gusev/Work/GK_Pult/padel_pult/.planning/phases/04-content-pages/04-PATTERNS.md
</execution_context>

<context>
@/Users/dmitry_gusev/Work/GK_Pult/padel_pult/.planning/ROADMAP.md

<interfaces>
<!-- users/index current state (lines 1-23, full file): -->
.container.py-4
  h1.mb-4 Пользователи

  - if @users.any?
    .table-responsive
      table.table.table-striped.table-hover.align-middle    ← line 6: remove .table-striped
        thead
          tr
            th #
            th Фото
            th Имя
            th Дата регистрации     ← email column goes AFTER this: th Email
        tbody
          - @users.each_with_index do |user, i|
            tr
              td= i + 1
              td
                = render "shared/avatar", user: user
              td= user.full_name
              td= user.created_at.strftime("%d.%m.%Y")    ← email cell goes AFTER this: td= user.email
  - else
    p.text-muted Пользователей пока нет.

<!-- Avatar partial calling convention: -->
= render "shared/avatar", user: user
<!-- user.email is always available — User model always has email (Devise requirement). No controller change needed. -->

<!-- Table analog from leagues/show.html.slim (Phase 3 complete, correct dark table pattern): -->
table.table.table-hover
  thead
    tr
      th Фото
      th Имя
      th Очки
  tbody
    - @league.league_users.order(score: :desc).each do |league_user|
      tr
        td
          = render "shared/avatar", user: league_user.user
        td= league_user.full_name
        td= league_user.score
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove .table-striped and add Email column to users/index</name>
  <files>app/views/users/index.html.slim</files>
  <read_first>
    - app/views/users/index.html.slim (current state — confirm line 6 has .table-striped and no email column exists)
  </read_first>
  <action>
In app/views/users/index.html.slim, make exactly 3 changes:

Change 1 — Line 6: `table.table.table-striped.table-hover.align-middle` → `table.table.table-hover.align-middle`

Change 2 — In thead, add `th Email` AFTER `th Имя` and BEFORE `th Дата регистрации`. The thead after change:
```slim
        thead
          tr
            th #
            th Фото
            th Имя
            th Email
            th Дата регистрации
```

Change 3 — In tbody, add `td= user.email` AFTER `td= user.full_name` and BEFORE `td= user.created_at.strftime(...)`. The tbody row after change:
```slim
            tr
              td= i + 1
              td
                = render "shared/avatar", user: user
              td= user.full_name
              td= user.email
              td= user.created_at.strftime("%d.%m.%Y")
```

ALL other content (.container.py-4, h1.mb-4, .table-responsive, avatar partial, empty state) must remain EXACTLY as-is.

IMPORTANT: The avatar partial call must remain `render "shared/avatar", user: user` — do not change the local variable name or add/remove parameters. The `user` variable comes from `@users.each_with_index do |user, i|`.
  </action>
  <verify>
    <automated>
      grep -n "table-striped\|user\.email\|th Email" /Users/dmitry_gusev/Work/GK_Pult/padel_pult/app/views/users/index.html.slim
    </automated>
    Expected: 0 lines with table-striped; 1 line with user.email; 1 line with "th Email"
  </verify>
  <acceptance_criteria>
    - `grep -c "table-striped" app/views/users/index.html.slim` returns 0
    - `grep -c "table\.table-hover\.align-middle" app/views/users/index.html.slim` returns 1
    - `grep -c "th Email" app/views/users/index.html.slim` returns 1
    - `grep -c "user\.email" app/views/users/index.html.slim` returns 1 (td= user.email in tbody)
    - `grep -c "shared/avatar" app/views/users/index.html.slim` returns 1 (avatar partial still rendered)
    - `grep -c "user\.full_name" app/views/users/index.html.slim` returns 1 (name column unchanged)
    - `grep -c "created_at\.strftime" app/views/users/index.html.slim` returns 1 (date column unchanged)
    - `grep -c "Пользователей пока нет" app/views/users/index.html.slim` returns 1 (empty state unchanged)
    - `grep -c "container\.py-4" app/views/users/index.html.slim` returns 1 (outer container unchanged)
    - `grep -c "table-responsive" app/views/users/index.html.slim` returns 1 (.table-responsive wrapper unchanged)
  </acceptance_criteria>
  <done>users/index: .table-striped removed; email column added in thead (after Имя) and tbody (after full_name); avatar partial, container, empty state all unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin-accessible route → Template | UsersController#index is admin-only; no template change affects access control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Information Disclosure | users/index.html.slim — adding email column | accept | Email is already accessible on User objects passed to this template. The route is admin-only and protected at controller level. Adding email column does not change who can access this page. |
</threat_model>

<verification>
After task completes:
```
grep -n "table-striped" app/views/users/index.html.slim
# Expected: 0 results

grep -n "user\.email\|th Email" app/views/users/index.html.slim
# Expected: 2 results (one th Email in thead, one td= user.email in tbody)

grep -n "table-hover" app/views/users/index.html.slim
# Expected: 1 result — table.table.table-hover.align-middle

# Cross-file shadow-sm final check:
grep -r "shadow-sm" app/views/
# Expected: 0 results across entire views directory
```
</verification>

<success_criteria>
- users/index: 0 occurrences of table-striped
- users/index: table uses .table.table-hover.align-middle
- users/index: thead has 5 columns: #, Фото, Имя, Email, Дата регистрации
- users/index: tbody row has 5 cells: index, avatar partial, full_name, email, created_at
- Overall: `grep -r "shadow-sm" app/views/` returns 0 results (all plans complete, all shadows removed)
</success_criteria>

<output>
After completion, create `.planning/phases/04-content-pages/04-05-SUMMARY.md`
</output>
