---
phase: 04-content-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/devise/sessions/new.html.slim
  - app/views/devise/registrations/new.html.slim
  - app/views/devise/registrations/edit.html.slim
  - app/views/devise/passwords/new.html.slim
  - app/views/devise/passwords/edit.html.slim
  - app/views/devise/confirmations/new.html.slim
  - app/views/devise/unlocks/new.html.slim
autonomous: true
requirements:
  - PAGE-02

must_haves:
  truths:
    - "All 7 auth pages render as a centered card on the dark background with no .shadow-sm class"
    - "All auth pages use .container.py-4 (not bare .container) as the outermost wrapper"
    - "All auth pages use .row.justify-content-center without .mt-5 (spacing handled by container py-4)"
    - "registrations/edit has both .card blocks without .shadow-sm (main form card and danger delete card)"
    - "registrations/edit uses .col-md-6 (wider than other auth pages at .col-md-5)"
  artifacts:
    - path: "app/views/devise/sessions/new.html.slim"
      provides: "Login page — card centered, no shadow"
      contains: ".container.py-4"
    - path: "app/views/devise/registrations/new.html.slim"
      provides: "Registration page — card centered, no shadow"
      contains: ".container.py-4"
    - path: "app/views/devise/registrations/edit.html.slim"
      provides: "Account edit page — two cards, no shadow"
      contains: ".container.py-4"
    - path: "app/views/devise/passwords/new.html.slim"
      provides: "Password reset request — card centered, no shadow"
      contains: ".container.py-4"
    - path: "app/views/devise/passwords/edit.html.slim"
      provides: "Password reset form — card centered, no shadow"
      contains: ".container.py-4"
    - path: "app/views/devise/confirmations/new.html.slim"
      provides: "Email confirmation resend — card centered, no shadow"
      contains: ".container.py-4"
    - path: "app/views/devise/unlocks/new.html.slim"
      provides: "Account unlock resend — card centered, no shadow"
      contains: ".container.py-4"
  key_links:
    - from: "all 7 auth templates"
      to: "Bootstrap dark card surface"
      via: "_cards.scss (background-color: var(--color-bg-surface))"
      pattern: "\\.card(?!\\.shadow)"
---

<objective>
Apply three-change fix to all 7 Devise auth templates: add `.py-4` to `.container`, remove `.mt-5` from inner `.row`, remove `.shadow-sm` from `.card`.

Purpose: Auth pages are the first thing unauthenticated users see. They must render with the correct dark card on dark background — centered, clean, with consistent vertical spacing from the container padding.
Output: 7 updated Devise templates, all using `.container.py-4 > .row.justify-content-center > .col-md-5 > .card > .card-body.p-4`.
</objective>

<execution_context>
@/Users/dmitry_gusev/Work/GK_Pult/padel_pult/.planning/phases/04-content-pages/04-RESEARCH.md
@/Users/dmitry_gusev/Work/GK_Pult/padel_pult/.planning/phases/04-content-pages/04-PATTERNS.md
</execution_context>

<context>
@/Users/dmitry_gusev/Work/GK_Pult/padel_pult/.planning/ROADMAP.md

<interfaces>
<!-- Canonical auth page scaffold (after changes) — applies to 6 of 7 pages: -->
.container.py-4
  .row.justify-content-center
    .col-md-5
      .card
        .card-body.p-4
          h2.card-title.mb-4 Page Title
          / ... form body UNCHANGED
          hr.my-3
          .text-center.small
            = render "devise/shared/links"

<!-- registrations/edit uses .col-md-6 (wider) — keep this difference -->
<!-- registrations/edit has TWO .card blocks — both need .shadow-sm removed -->
<!-- The second card (.card.shadow-sm.mt-3 → .card.mt-3) is a sibling AFTER the .row block, at .container level -->

<!-- EXACT current state of each file line 1-4 (only these 4 lines change): -->
<!-- sessions/new:        line 1: .container | line 2: .row.justify-content-center.mt-5 | line 4: .card.shadow-sm -->
<!-- registrations/new:   line 1: .container | line 2: .row.justify-content-center.mt-5 | line 4: .card.shadow-sm -->
<!-- registrations/edit:  line 1: .container | line 2: .row.justify-content-center.mt-5 | line 4: .card.shadow-sm | line 49: .card.shadow-sm.mt-3 -->
<!-- passwords/new:       line 1: .container | line 2: .row.justify-content-center.mt-5 | line 4: .card.shadow-sm -->
<!-- passwords/edit:      line 1: .container | line 2: .row.justify-content-center.mt-5 | line 4: .card.shadow-sm -->
<!-- confirmations/new:   line 1: .container | line 2: .row.justify-content-center.mt-5 | line 4: .card.shadow-sm -->
<!-- unlocks/new:         line 1: .container | line 2: .row.justify-content-center.mt-5 | line 4: .card.shadow-sm -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix container + shadow on 6 simple auth pages</name>
  <files>
    app/views/devise/sessions/new.html.slim,
    app/views/devise/registrations/new.html.slim,
    app/views/devise/passwords/new.html.slim,
    app/views/devise/passwords/edit.html.slim,
    app/views/devise/confirmations/new.html.slim,
    app/views/devise/unlocks/new.html.slim
  </files>
  <read_first>
    - app/views/devise/sessions/new.html.slim
    - app/views/devise/registrations/new.html.slim
    - app/views/devise/passwords/new.html.slim
    - app/views/devise/passwords/edit.html.slim
    - app/views/devise/confirmations/new.html.slim
    - app/views/devise/unlocks/new.html.slim
  </read_first>
  <action>
In each of these 6 files, make exactly 3 line changes — ALL other content must remain byte-for-byte identical:

Change 1 — Line 1: `.container` → `.container.py-4`
Change 2 — Line 2: `.row.justify-content-center.mt-5` → `.row.justify-content-center`
Change 3 — Line 4: `.card.shadow-sm` → `.card`

Apply to all 6 files:
- app/views/devise/sessions/new.html.slim
- app/views/devise/registrations/new.html.slim
- app/views/devise/passwords/new.html.slim
- app/views/devise/passwords/edit.html.slim
- app/views/devise/confirmations/new.html.slim
- app/views/devise/unlocks/new.html.slim

DO NOT change form_for helpers, field names, label text, submit button text, or any other content.
  </action>
  <verify>
    <automated>
      grep -rn "shadow-sm\|\.container[^.]" app/views/devise/sessions/new.html.slim app/views/devise/passwords/new.html.slim app/views/devise/passwords/edit.html.slim app/views/devise/confirmations/new.html.slim app/views/devise/unlocks/new.html.slim app/views/devise/registrations/new.html.slim
    </automated>
    Expected: Only lines containing `.container.py-4` — NO lines containing `.container` alone or `shadow-sm`
  </verify>
  <acceptance_criteria>
    - `grep -c "shadow-sm" app/views/devise/sessions/new.html.slim` returns 0
    - `grep -c "shadow-sm" app/views/devise/registrations/new.html.slim` returns 0
    - `grep -c "shadow-sm" app/views/devise/passwords/new.html.slim` returns 0
    - `grep -c "shadow-sm" app/views/devise/passwords/edit.html.slim` returns 0
    - `grep -c "shadow-sm" app/views/devise/confirmations/new.html.slim` returns 0
    - `grep -c "shadow-sm" app/views/devise/unlocks/new.html.slim` returns 0
    - `grep -c "\.container\.py-4" app/views/devise/sessions/new.html.slim` returns 1
    - `grep -c "\.container\.py-4" app/views/devise/passwords/new.html.slim` returns 1
    - `grep -c "mt-5" app/views/devise/sessions/new.html.slim` returns 0
    - `grep -c "mt-5" app/views/devise/passwords/new.html.slim` returns 0
    - `grep -c "card-body.p-4" app/views/devise/sessions/new.html.slim` returns 1 (content unchanged)
    - `grep -c "Войти" app/views/devise/sessions/new.html.slim` returns 1 (h2 title unchanged)
    - `grep -c "btn btn-primary" app/views/devise/sessions/new.html.slim` returns 1 (submit unchanged)
    - `grep -c "devise/shared/links" app/views/devise/sessions/new.html.slim` returns 1 (links partial unchanged)
  </acceptance_criteria>
  <done>6 auth pages: .container.py-4 on line 1, .row.justify-content-center (no .mt-5) on line 2, .card (no .shadow-sm) on line 4. All form content unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Fix container + two shadows on registrations/edit</name>
  <files>app/views/devise/registrations/edit.html.slim</files>
  <read_first>
    - app/views/devise/registrations/edit.html.slim (current state — read before modifying)
  </read_first>
  <action>
In app/views/devise/registrations/edit.html.slim, make exactly 4 line changes:

Change 1 — Line 1: `.container` → `.container.py-4`
Change 2 — Line 2: `.row.justify-content-center.mt-5` → `.row.justify-content-center`
Change 3 — Line 4: `.card.shadow-sm` → `.card`
Change 4 — Line 49: `.card.shadow-sm.mt-3` → `.card.mt-3`

Note on structure: this file uses `.col-md-6` (wider than other auth pages — keep it at `.col-md-6`).
The second card at line 49 (`.card.shadow-sm.mt-3`) is a sibling element AFTER the closing of the `.row.justify-content-center` block. It remains at the `.container.py-4` level — do not move or re-indent it.

ALL other content (avatar partial render, form fields, error messages, delete button) must remain EXACTLY as-is.
  </action>
  <verify>
    <automated>grep -c "shadow-sm" /Users/dmitry_gusev/Work/GK_Pult/padel_pult/app/views/devise/registrations/edit.html.slim</automated>
    Expected output: 0
  </verify>
  <acceptance_criteria>
    - `grep -c "shadow-sm" app/views/devise/registrations/edit.html.slim` returns 0
    - `grep -c "\.container\.py-4" app/views/devise/registrations/edit.html.slim` returns 1
    - `grep -c "mt-5" app/views/devise/registrations/edit.html.slim` returns 0
    - `grep -c "\.col-md-6" app/views/devise/registrations/edit.html.slim` returns 1 (wider col preserved)
    - `grep -c "\.card\.mt-3" app/views/devise/registrations/edit.html.slim` returns 1 (second card present with mt-3)
    - `grep -c "shared/avatar" app/views/devise/registrations/edit.html.slim` returns 1 (avatar partial unchanged)
    - `grep -c "btn-danger" app/views/devise/registrations/edit.html.slim` returns 1 (delete button unchanged)
    - `grep -c "Редактировать аккаунт" app/views/devise/registrations/edit.html.slim` returns 1 (h2 title unchanged)
  </acceptance_criteria>
  <done>registrations/edit: .container.py-4, .row.justify-content-center (no mt-5), both .card blocks have .shadow-sm removed; col-md-6, avatar partial, delete card, all form fields preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Unauthenticated user → Devise forms | Forms submit credentials; Devise handles CSRF and authentication — no template change affects this |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | Devise form templates | accept | Changes are purely cosmetic (CSS class removal). Form actions, CSRF tokens, and form_for helpers are untouched. Devise security is enforced server-side. |
</threat_model>

<verification>
After both tasks complete, run this cross-file check:
```
grep -rn "shadow-sm" app/views/devise/
# Expected: 0 results

grep -rn "\.container\.py-4" app/views/devise/sessions/ app/views/devise/registrations/ app/views/devise/passwords/ app/views/devise/confirmations/ app/views/devise/unlocks/
# Expected: 7 results (one per file)

grep -rn "mt-5" app/views/devise/
# Expected: 0 results
```
</verification>

<success_criteria>
- All 7 auth templates: 0 occurrences of shadow-sm
- All 7 auth templates: outer wrapper is .container.py-4 (not bare .container)
- All 7 auth templates: .row.justify-content-center without .mt-5
- registrations/edit: two .card blocks (main + delete danger), both without shadow-sm, col-md-6 preserved
- All form fields, labels, submit buttons, and Devise partial renders unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/04-content-pages/04-02-SUMMARY.md`
</output>
