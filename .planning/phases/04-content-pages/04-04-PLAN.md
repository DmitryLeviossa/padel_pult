---
phase: 04-content-pages
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/tournaments/index.html.slim
autonomous: true
requirements:
  - PAGE-04

must_haves:
  truths:
    - "tournaments/index uses .container.py-4 (not .container.mt-4)"
    - "tournaments/index table has no .table-striped class — uses .table.table-hover only"
    - "tournaments/show and tournaments/new are not modified (already complete)"
  artifacts:
    - path: "app/views/tournaments/index.html.slim"
      provides: "Tournaments index with correct container and dark table"
      contains: ".container.py-4"
  key_links:
    - from: "tournaments/index .container.py-4"
      to: "main.py-4 in layout"
      via: "vertical spacing from container padding-y, not margin-top"
      pattern: "\\.container\\.py-4"
    - from: "table.table.table-hover"
      to: "_tables.scss dark header"
      via: "Bootstrap CSS cascade — thead styled by Phase 3 _tables.scss"
      pattern: "table\\.table\\.table-hover(?!\\.table-striped)"
---

<objective>
Apply two targeted fixes to tournaments/index.html.slim: change .container.mt-4 to .container.py-4 and remove .table-striped from the table. tournaments/show and tournaments/new are already complete and must not be touched.

Purpose: Tournaments index is the only tournament template with spacing and table consistency issues. Both fixes are single-line changes.
Output: Updated tournaments/index.html.slim with consistent container padding and a dark-consistent table without striped rows.
</objective>

<execution_context>
@/Users/dmitry_gusev/Work/GK_Pult/padel_pult/.planning/phases/04-content-pages/04-RESEARCH.md
@/Users/dmitry_gusev/Work/GK_Pult/padel_pult/.planning/phases/04-content-pages/04-PATTERNS.md
</execution_context>

<context>
@/Users/dmitry_gusev/Work/GK_Pult/padel_pult/.planning/ROADMAP.md

<interfaces>
<!-- tournaments/index current state (lines 1-29, full file): -->
.container.mt-4                         ← line 1: change to .container.py-4
  h1.mb-4 Турниры

  - if @tournaments.any?
    table.table.table-striped.table-hover   ← line 5: change to table.table.table-hover
      thead
        tr
          th Название
          th Место
          th Дата начала
          th Дата окончания
          th Тип
          th Статус
          th Макс. участников
      tbody
        - @tournaments.each do |tournament|
          tr
            td= tournament.name
            td= tournament.location.presence || "—"
            td= tournament.start_date
            td= tournament.end_date
            td= tournament.type.capitalize
            td
              - badge_class = { "draft" => "secondary", "active" => "success", "completed" => "primary", "cancelled" => "danger" }.fetch(tournament.status, "secondary")
              span.badge class="bg-#{badge_class}"
                = tournament.status.capitalize
            td.text-center= tournament.max_participants
  - else
    p.text-muted Турниров пока нет.

<!-- tournaments/show — DO NOT TOUCH. Already complete from Phase 3 audit. -->
<!-- tournaments/new  — DO NOT TOUCH. Already complete from Phase 3 audit. -->

<!-- Why drop .table-striped: _tables.scss overrides thead background via var(--color-bg-surface).
     Bootstrap's .table-striped uses --bs-table-striped-bg which is NOT overridden in project tokens.
     The striped alternating rows use a different shade that doesn't match the surface token,
     creating visual inconsistency. .table-hover is sufficient for interactivity. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix container and table class in tournaments/index</name>
  <files>app/views/tournaments/index.html.slim</files>
  <read_first>
    - app/views/tournaments/index.html.slim (current state — confirm line 1 is .container.mt-4 and line 5 has .table-striped)
  </read_first>
  <action>
In app/views/tournaments/index.html.slim, make exactly 2 line changes:

Change 1 — Line 1: `.container.mt-4` → `.container.py-4`
Change 2 — Line 5: `table.table.table-striped.table-hover` → `table.table.table-hover`

ALL other content must remain EXACTLY as-is:
- h1.mb-4 Турниры
- All thead th cells (Название, Место, Дата начала, etc.)
- All tbody td cells including badge interpolation logic
- Empty state `p.text-muted Турниров пока нет.`
  </action>
  <verify>
    <automated>
      grep -n "container\|table-striped" /Users/dmitry_gusev/Work/GK_Pult/padel_pult/app/views/tournaments/index.html.slim
    </automated>
    Expected: line 1 shows `.container.py-4`; no line shows `table-striped`; no line shows `container.mt-4`
  </verify>
  <acceptance_criteria>
    - `grep -c "container\.mt-4" app/views/tournaments/index.html.slim` returns 0
    - `grep -c "container\.py-4" app/views/tournaments/index.html.slim` returns 1
    - `grep -c "table-striped" app/views/tournaments/index.html.slim` returns 0
    - `grep -c "table\.table-hover" app/views/tournaments/index.html.slim` returns 1
    - `grep -c "Название" app/views/tournaments/index.html.slim` returns 1 (th cell unchanged)
    - `grep -c "badge_class" app/views/tournaments/index.html.slim` returns 1 (badge logic unchanged)
    - `grep -c "Турниров пока нет" app/views/tournaments/index.html.slim` returns 1 (empty state unchanged)
    - `grep -c "max_participants" app/views/tournaments/index.html.slim` returns 1 (last column unchanged)
  </acceptance_criteria>
  <done>tournaments/index: line 1 is .container.py-4, table uses .table.table-hover (no .table-striped); all other content unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Controller → Template | @tournaments variable is set by TournamentsController — no auth-sensitive changes in this template |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Information Disclosure | tournaments/index.html.slim | accept | Pure CSS class change. No data access pattern change. Tournament data visibility is governed by controller, not template class names. |
</threat_model>

<verification>
After task completes:
```
grep -n "container" app/views/tournaments/index.html.slim
# Expected: line 1 shows .container.py-4 only

grep -n "table-striped" app/views/tournaments/index.html.slim
# Expected: 0 results

# Confirm show and new are untouched:
git diff app/views/tournaments/show.html.slim app/views/tournaments/new.html.slim
# Expected: no diff output (files unchanged)
```
</verification>

<success_criteria>
- tournaments/index line 1: .container.py-4 (not .container.mt-4)
- tournaments/index has 0 occurrences of table-striped
- tournaments/index table uses .table.table-hover
- tournaments/show and tournaments/new: no changes (git diff shows nothing)
</success_criteria>

<output>
After completion, create `.planning/phases/04-content-pages/04-04-SUMMARY.md`
</output>
